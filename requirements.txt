# Industrial Predictive Maintenance Dashboard — dependencies
# Tested with Python 3.10 / 3.11
#
# NOTE ON PYTORCH: the line below installs the standard PyPI build of torch.
# If you want the smaller CPU-only wheel instead, install it first with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then run `pip install -r requirements.txt` (pip will see torch is already
# satisfied and skip it).

streamlit>=1.35,<2.0
pandas>=2.0
numpy>=1.24,<2.0
plotly>=5.20

tensorflow>=2.16,<2.17
torch>=2.2,<2.4

scikit-learn>=1.3
shap>=0.45

openpyxl>=3.1
python-docx>=1.1
